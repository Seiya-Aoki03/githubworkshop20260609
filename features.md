# 実装する必要のある機能一覧

## 1. 必須機能

### 画面表示
- ポモドーロタイマーのメイン画面を表示する
- 残り時間を大きく見やすく表示する
- 現在のモードを表示する
- 開始、停止、一時停止、リセットの操作ボタンを表示する
- 作業時間と休憩時間の設定欄を表示する

### タイマー操作
- タイマーを開始できるようにする
- タイマーを一時停止できるようにする
- タイマーを再開できるようにする
- タイマーをリセットできるようにする
- 作業時間と休憩時間を切り替えられるようにする

### カウントダウン処理
- 1 秒ごとに残り時間を更新する
- 残り時間が 0 になったら次のフェーズへ進める
- 作業時間、短い休憩、長い休憩を切り替える
- セッション数をカウントする

### 状態管理
- 実行中、停止中、一時停止中を管理する
- 作業中、短い休憩中、長い休憩中を管理する
- 現在の残り秒数を管理する
- 現在のセッション数を管理する

## 2. 設定機能

- 作業時間を変更できるようにする
- 短い休憩時間を変更できるようにする
- 長い休憩時間を変更できるようにする
- 何回作業したら長い休憩に入るかを変更できるようにする
- 入力値のバリデーションを行う

## 3. 保存機能

- ブラウザ再読込後も状態を復元できるようにする
- 現在の状態を localStorage に保存する
- 保存した設定値を読み込めるようにする
- 将来的にサーバー保存へ拡張できる形にする

## 4. 通知機能

- フェーズ終了時に画面上で分かる通知を出す
- 必要に応じて音で通知できるようにする
- ブラウザ通知へ拡張できる余地を持たせる

## 5. 画面改善機能

- モックに沿ったデザインを再現する
- スマートフォンでも操作しやすいレスポンシブ対応を行う
- 状態に応じてボタンや表示の見た目を変える
- 必要なら履歴やステータス表示を追加する

## 6. Flask 側の機能

- ルート画面を返す
- 設定取得用の API を用意する
- 設定保存用の API を用意する
- 将来の履歴保存や統計取得に備えた拡張口を残す

## 7. テストしやすさのための機能分離

- タイマーの状態遷移ロジックを DOM から分離する
- 現在時刻の取得処理を注入できるようにする
- 保存処理を独立した層に分ける
- 設定の検証処理を独立させる
- 表示更新処理と計算処理を分ける

## 8. 実装優先順位

### 最優先
- 画面表示
- 開始、停止、一時停止、リセット
- 残り時間の更新
- 作業時間と休憩時間の切り替え

### 次の段階
- 設定変更
- localStorage への保存と復元
- 見た目の調整

### 後から追加
- 通知機能
- 履歴機能
- 統計機能
- サーバー保存

## 9. 完成条件の目安

- タイマーを開始して、時間が進むことが確認できる
- 作業終了後に休憩へ切り替わる
- 停止、一時停止、再開、リセットが正しく動作する
- 設定値を変更できる
- 再読込後に状態をある程度復元できる
- 画面がモックに近い見た目になっている
