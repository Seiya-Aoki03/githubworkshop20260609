# 段階的実装計画

段階的に進めるなら、機能を横に広く切るのではなく、1段階ごとに「画面・ロジック・最低限の保存」まで動く縦割りの粒度にするのがよいです。そうすると、各段階で必ず動作確認できて、ユニットテストも書きやすくなります。

## 実装フェーズ案

### フェーズ1: 画面の骨組みを作る
- 目的: UI モックに近い静的画面を出す
- 実装内容:
  - Flask でトップ画面を返す
  - HTML でタイマー表示、操作ボタン、設定領域を配置する
  - CSS でレイアウトと見た目を整える
- 完成条件:
  - 画面が表示される
  - ボタンや表示領域の位置関係が決まっている
- この段階ではまだタイマーは動かさない

### フェーズ2: タイマーの最小動作を作る
- 目的: 開始からカウントダウンまでを成立させる
- 実装内容:
  - 開始ボタンで 1 秒単位のカウントダウンを開始する
  - 一時停止、再開、リセットを実装する
  - 残り時間の表示更新を行う
- 完成条件:
  - 作業時間が減っていく
  - 停止・再開・リセットができる
- この段階では保存や通知はまだ不要

### フェーズ3: 状態遷移を完成させる
- 目的: ポモドーロらしい動作を成立させる
- 実装内容:
  - 作業時間終了後に休憩へ切り替える
  - 短い休憩と長い休憩を分ける
  - セッション数を管理する
  - モード表示を更新する
- 完成条件:
  - 作業と休憩が自動で切り替わる
  - 何セット目かが追える
- ここで状態機械としての完成度が上がる

### フェーズ4: 設定変更と保存を入れる
- 目的: 使い勝手を上げる
- 実装内容:
  - 作業時間、短い休憩、長い休憩、長休憩までの回数を変更できるようにする
  - localStorage に状態と設定を保存する
  - 再読込時に復元する
- 完成条件:
  - 設定を変えても動作が反映される
  - 更新後も途中状態がある程度戻る
- この段階で「実用最小版」になります

### フェーズ5: モック準拠の磨き込み
- 目的: 見た目と操作感を完成させる
- 実装内容:
  - 配色、余白、フォント、ボタン状態を調整する
  - スマホ表示を整える
  - 状態に応じた表示の変化をつける
- 完成条件:
  - モックに近い見た目になる
  - 小さい画面でも破綻しない

### フェーズ6: 拡張機能を追加する
- 目的: 今後の発展に備える
- 実装内容:
  - 通知音やブラウザ通知
  - 履歴
  - 統計
  - サーバー保存用 API
- 完成条件:
  - 基本機能を壊さずに拡張できる

## 粒度の考え方
- 1フェーズは「ユーザーが価値を感じる最小単位」にする
- 1フェーズの中で、HTML・CSS・JavaScript・Flask を必要最小限だけ連携させる
- まず純粋ロジックを切り出して、その上に UI と保存を乗せる
- 1フェーズ終わるごとに、最低1つはテストできる状態にする

## おすすめの実装順
- 画面
- 開始・停止・再開・リセット
- 時間切り替え
- 設定
- 保存
- 見た目調整
- 通知や履歴

この順番だと、architecture.md で整理した「Flask は薄く、タイマー本体は JavaScript の純粋ロジック」という方針と整合しますし、features.md の内容も無理なく分割できます。

必要なら次に、これをそのまま作業計画に使えるように「各フェーズで作るファイル」と「各フェーズのテスト項目」まで落とし込みます。
