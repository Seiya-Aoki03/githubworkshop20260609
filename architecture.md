# Webアプリケーションアーキテクチャ案

## 目的
このプロジェクトでは、Flask と HTML/CSS/JavaScript を用いてポモドーロタイマーの Web アプリを実装する。
UI モックに沿った見た目を再現しつつ、将来的な機能追加とユニットテストのしやすさを両立することを目的とする。

## 基本方針
- Flask は画面配信と API の入口に限定する。
- タイマーの状態遷移はブラウザ側の JavaScript で管理する。
- タイマーの本体ロジックは DOM から切り離し、純粋な状態管理として実装する。
- 設定値、保存処理、表示処理はそれぞれ分離する。
- まずはシンプルに作り、必要に応じて永続化や履歴機能を追加する。

## 採用する構成
### 1. Flask 層
Flask は以下の責務のみを持つ。
- `GET /` で HTML を返す。
- 必要に応じて設定取得・保存用の API を提供する。
- 今後の拡張として、履歴保存や統計取得の API を追加できるようにする。

Flask 側でタイマーのカウントダウンや状態遷移を持たない。
これにより、ルーティングのテストとタイマーのロジックテストを分離できる。

### 2. テンプレート層
`templates/index.html` に画面の骨組みを置く。
- タイマー表示
- 開始、停止、リセットなどの操作ボタン
- 作業時間や休憩時間の設定欄
- 必要なら履歴やステータス表示

画面構造だけを持たせ、計算ロジックは JavaScript 側へ寄せる。

### 3. CSS 層
`static/css/style.css` にレイアウトと見た目をまとめる。
- モックに沿った配色、余白、フォント、ボタンデザインを実装する。
- スマートフォン表示を含むレスポンシブ対応を行う。
- 状態に応じた見た目の差分をクラスで表現する。

### 4. JavaScript 層
`static/js/app.js` を UI の制御とタイマー状態管理の中心にする。

JavaScript の責務は以下。
- タイマー開始、停止、一時停止、リセットの操作
- 残り時間の更新
- 作業時間から休憩時間への状態遷移
- localStorage への状態保存と復元
- DOM 更新

ただし、状態計算と DOM 更新は分ける。
状態遷移の判定はテスト可能な関数やクラスに閉じ込め、DOM 操作は薄く保つ。

## ドメイン設計
タイマーの中心ロジックは、状態機械として表現する。

### 状態
- `idle`: 停止中
- `running`: 実行中
- `paused`: 一時停止中
- `work`: 作業時間
- `short_break`: 短い休憩
- `long_break`: 長い休憩

実装上は、`status` と `mode` を分けると扱いやすい。
- `status` は実行状態
- `mode` は作業フェーズ

### 管理するデータ
- 現在のモード
- 現在の状態
- 残り秒数
- セッション数
- 設定値
- 終了予定時刻

### 状態遷移の例
- 開始すると `idle` から `running` になる。
- 一時停止すると `running` から `paused` になる。
- 再開すると `paused` から `running` になる。
- 残り時間が 0 になると次のフェーズへ進む。
- リセットすると初期状態へ戻る。

## テストしやすさを高める追加設計
ユニットテストを容易にするため、次の分離を行う。

### 1. 時計の注入
現在時刻を直接参照せず、時計オブジェクトまたは関数を注入する。
これにより、テストでは固定時刻を使って挙動を再現できる。

### 2. 保存層の分離
localStorage や将来の DB 保存を直接ロジックに書かず、保存用の層を分ける。
- 保存失敗時の挙動を検証しやすい。
- 実装後の永続化先変更にも強い。

### 3. 設定の独立
作業時間、短休憩、長休憩、長休憩までの回数を設定オブジェクトにまとめる。
バリデーションも別関数に分離すると、境界値テストがしやすい。

### 4. 状態遷移ロジックの純粋化
DOM へ依存しない純粋関数または小さなクラスとして実装する。
これにより、操作と結果の対応を単体テストで確認できる。

## 推奨ディレクトリ構成
```text
1.pomodoro/
  app.py
  templates/
    index.html
  static/
    css/
      style.css
    js/
      app.js
    img/
      pomodoro.png
```

必要に応じて、将来的には次のような分割も検討できる。
```text
1.pomodoro/
  app.py
  pomodoro/
    __init__.py
    routes.py
    domain.py
    services.py
    storage.py
    settings.py
  templates/
  static/
```

## 実装の進め方
1. Flask で画面を表示できる最小構成を作る。
2. JavaScript でタイマーの開始、停止、リセットを実装する。
3. CSS で UI モックに合わせて見た目を整える。
4. localStorage で状態を保存し、再読込に耐えるようにする。
5. 必要になった時点で API、履歴、統計を追加する。

## テスト方針
### 単体テスト
- 状態遷移
- 時間計算
- 設定値のバリデーション
- 保存データの読み書き

### 統合テスト
- Flask のルートが正しく HTML を返すこと
- 必要なら API が期待通りに動くこと

### UI テスト
- ボタン操作で表示が更新されること
- フェーズ切り替え時に見た目が変わること

## この設計の利点
- Flask を薄く保てるため、保守しやすい。
- タイマー本体を純粋ロジックとして扱えるため、ユニットテストがしやすい。
- 将来 API や履歴機能を追加しても、既存コードの変更範囲を抑えやすい。
- UI モックに合わせた実装を進めやすい。

## 結論
このプロジェクトでは、Flask は配信と API の入口に限定し、タイマーの状態管理は JavaScript の純粋ロジックとして分離する構成が最適である。
特に、時計の注入、保存層の分離、設定の独立を入れておくと、実装後のユニットテストが大幅にやりやすくなる。
